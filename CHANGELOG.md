# บันทึกการเปลี่ยนแปลง (Changelog)

รูปแบบเวอร์ชันตาม [Semantic Versioning](https://semver.org/lang/th/)

## [1.9.0] - 2026-07-24

### 🔀 ย้ายแผนกไปฝ่ายอื่นได้ (พร้อมย้ายคน/ตำแหน่งตามไป)
- หน้าจัดการองค์กร (`/admin/org`) เพิ่มเมนู **"↔ ย้ายฝ่าย…"** ในแต่ละแผนก (เฉพาะแอดมิน)
- เมื่อย้ายแผนก → **ตำแหน่ง · ผู้ใช้ · พนักงาน** ทั้งหมดในแผนกนั้นย้ายฝ่ายตามไปอัตโนมัติ (กันข้อมูลฝ่ายไม่ตรงกัน)
- ผู้ใช้ที่ถูกย้ายจะถูกบังคับให้เข้าสู่ระบบใหม่ 1 ครั้ง (รีเฟรชสิทธิ์ให้ตรงฝ่ายใหม่) · กันย้ายทับชื่อแผนกที่ซ้ำในฝ่ายปลายทาง

### 👥 หัวหน้าแผนก / หัวหน้าฝ่าย / ผู้จัดการ ดูแลได้หลายแผนก/ฝ่าย
- ขยายระบบ "ดูแลเพิ่มเติม" (scope override) ที่เดิมมีเฉพาะผู้จัดการ → ให้ **หัวหน้าฝ่าย + หัวหน้าแผนก** ใช้ได้ด้วย
- ในหน้าเพิ่ม/แก้ผู้ใช้ เมื่อเลือก role เหล่านี้จะมีกล่อง **"🗂️ ดูแลเพิ่มเติม (หลายแผนก/หลายฝ่าย)"** ให้แอดมินติ๊กฝ่าย/แผนก/ตำแหน่งเพิ่ม
- ครอบคลุมทั้ง ดูบันทึกงานทีม · จัดกะ · รายงาน (ผ่าน canView/canEdit เดียวกัน) · หัวหน้างาน(supervisor)/เจ้าหน้าที่ ไม่ได้รับสิทธิ์นี้
- ทดสอบแล้ว: E2E 24 เคส (ย้ายแผนก cascade + บังคับ login ใหม่ + non-admin block · หัวหน้าเห็น/จัดกะข้ามแผนก-ข้ามฝ่ายตาม override)
> ไม่มี dependency ใหม่ · `git pull && npm install --omit=dev && pm2 restart hr-interview`

## [1.8.3] - 2026-07-24

### 🔎 หน้าจัดการผู้ใช้ — ค้นหา + กรองฝ่าย/แผนก
- เพิ่มแถบเหนือตารางรายชื่อ: **ช่องค้นหาชื่อ/username** + **เมนูเลือกฝ่าย** + **เมนูเลือกแผนก** (แผนก cascade ตามฝ่ายที่เลือก) + ปุ่ม "ล้าง"
- แสดงจำนวนผลลัพธ์ "แสดง X จาก Y คน" · ค้นหาไม่พบมีข้อความบอก
- ตารางเลื่อนแนวนอนได้บนจอแคบ (มือถือ)
- (ฟอร์มเพิ่ม/แก้ผู้ใช้ มีเมนู dropdown ฝ่าย/แผนก/ตำแหน่ง แบบ cascade อยู่แล้ว)
> ปรับ UI หน้า `/admin/users` ล้วน · `git pull && npm install --omit=dev && pm2 restart hr-interview`

## [1.8.2] - 2026-07-24

### 🧭 แยก "จัดกะ" และ "หมวดหมู่งาน" เป็นเมนูของตัวเอง (หัวหน้าหาง่าย ไม่งง)
- เพิ่มเมนู **🗓️ จัดกะ / ตารางเวร** และ **🏷️ หมวดหมู่งาน** ในแถบเมนูด้านซ้าย (เดิมซ่อนเป็นปุ่มในหน้า "งานของทีม")
- **จัดกะ / ตารางเวร** เห็นเฉพาะ แอดมิน · ผู้บริหาร · ผู้จัดการ · หัวหน้าฝ่าย · หัวหน้าแผนก
- **หมวดหมู่งาน** เห็นเฉพาะ ผู้จัดการ · หัวหน้าฝ่าย · หัวหน้าแผนก
- กล่อง "วิธีใช้หน้านี้" ในหน้าจัดกะ อธิบายชัด: หัวหน้าจัดกะให้พนักงานได้เอง แต่การเพิ่ม/แก้ **แม่แบบกะ (ช่วงเวลา)** ทำได้เฉพาะแอดมิน (ตามที่กำหนด)
- ลบปุ่มซ้ำในหน้า "งานของทีม" (ย้ายเข้าเมนูแล้ว)
> เป็นการปรับ UI ล้วน (สิทธิ์เดิมไม่เปลี่ยน) · bump cache `app-shell.js?v=4` ให้แล้ว — หลัง deploy ผู้ใช้จะเห็นเมนูใหม่ทันที · `git pull && npm install --omit=dev && pm2 restart hr-interview`

## [1.8.1] - 2026-07-24

### 🔧 กู้อัตโนมัติ: คนที่ "ค้าง" อยู่แล้วจากบั๊กเดิม
- ต่อยอดจาก v1.8.0 — ผู้ที่ทำสัมภาษณ์จบไปก่อนหน้านี้ (กดจบแล้วจริง) แต่ค้างสถานะ "กำลังตอบ" เพราะเอกสารล้มในเวอร์ชันเก่า จะถูกเลื่อนเป็น "เสร็จ" ให้อัตโนมัติเมื่อเปิดหน้าหลักครั้งถัดไป — ไม่ต้องให้แอดมินแก้ทีละคน
- ปลอดภัย: เลื่อนเฉพาะสัมภาษณ์ที่มี `finishedAt` (กดจบแล้วจริง) เท่านั้น — คนที่ตอบยังไม่จบจะยังคงสถานะ "กำลังตอบ" ตามเดิม
- ทดสอบแล้ว: E2E ยืนยันทั้งเคสกู้ (จบแล้ว→เสร็จ) และเคสไม่กู้ (ตอบไม่จบ→คงเดิม)

## [1.8.0] - 2026-07-24

### 🐛 แก้บั๊ก: สัมภาษณ์ถูกให้ทำซ้ำ (ทั้งที่ควรทำครั้งเดียว)
- **อาการ:** ผู้ใช้ทำสัมภาษณ์รอบแรกไปแล้ว แต่วันรุ่งขึ้นระบบยังให้ "ตอบต่อ" เหมือนต้องทำซ้ำ
- **ต้นเหตุ:** เดิมสถานะสัมภาษณ์จะกลายเป็น "เสร็จ" ก็ต่อเมื่อ AI สร้างเอกสาร JD/KPI สำเร็จเท่านั้น — ถ้าสร้างเอกสารช้า/ค้าง/ล้มเหลว (เน็ต, timeout, quota) สถานะจะค้างที่ "กำลังตอบ" → หน้าหลักโชว์ปุ่ม "ตอบต่อจากที่ค้างไว้"
- **แก้:** ถือว่าสัมภาษณ์ "เสร็จ" ทันทีที่ตอบครบและกดจบ (แยกออกจากขั้นสร้างเอกสารโดยสิ้นเชิง) → สัมภาษณ์ทำ **ครั้งเดียวจริง** ต่อให้เอกสารล้มก็แค่กด "🔄 สร้างเอกสารใหม่" (ไม่ต้องตอบใหม่)
- หน้าหลักแสดงสถานะเอกสารแยกชัด: ⏳ กำลังสร้าง / ⚠️ สร้างไม่สำเร็จ (มีปุ่มสร้างใหม่) / 📄 ดูเอกสารได้

### 🏷️ หัวหน้าเพิ่ม/จัดการหมวดหมู่ของฝ่ายตัวเองได้
- หน้าใหม่ **จัดการหมวดหมู่งาน** (`/manage/categories`) สำหรับ **ผู้จัดการ · หัวหน้าฝ่าย · หัวหน้าแผนก** — เพิ่ม/แก้/ลบ หมวดได้เฉพาะ "ฝ่ายตัวเอง" (หมวดกลางทุกฝ่ายยังเป็นแอดมินเท่านั้น)
- หมวดที่หัวหน้าเพิ่มถูกเก็บใน **ฝ่ายนั้นๆ** และพนักงานในฝ่ายเลือกได้ทันทีในหน้าบันทึกงาน
- ลิงก์ "🏷️ จัดการหมวดหมู่" อยู่ในหน้า "งานของทีม"
- หัวหน้างาน(supervisor)/เจ้าหน้าที่ = read-only ตามดีไซน์เดิม (ยัง quick-add หมวดผ่านหน้าบันทึกงานได้เหมือนเดิม)
- แก้/ลบข้ามฝ่าย หรือแตะหมวดกลาง = ถูกบล็อก (403)
- ทดสอบแล้ว: E2E 23 เคส (รวมพิสูจน์บั๊กสัมภาษณ์: จำลองเอกสารล้ม → สถานะยังเป็น completed)
> ไม่มี dependency ใหม่ · `cd /opt/hr-interview && git pull && npm install --omit=dev && pm2 restart hr-interview`

## [1.7.1] - 2026-07-24

### ระบบกะ — เฟส 2c (รายงานคิดตามกะ) · ครบทั้งระบบกะแล้ว
- หน้า **สรุปบันทึกงาน** คำนวณ "ความครบ (%)" โดยใช้ **ชั่วโมงที่คาดหวังตามกะของแต่ละวัน** (เดิมใช้เวลาทำงานคงที่) — แม่นยำขึ้นเมื่อพนักงานเข้ากะต่างกันในแต่ละวัน (โหลดตารางกะครั้งเดียวต่อคน ไม่กระทบ performance)
- ทดสอบแล้ว: E2E ยืนยันความครบเฉลี่ยคิดตามกะรายวัน (วันเข้ากะสั้น = ชั่วโมงคาดหวังน้อยลง)
> **ระบบกะครบทั้ง 3 เฟส:** 2a แม่แบบกะ + บันทึกงานอ่านกะ · 2b ปฏิทินจัดกะ · 2c รายงานตามกะ
> ไม่มี dependency ใหม่ · `cd /opt/hr-interview && git pull && npm install --omit=dev && pm2 restart hr-interview`

## [1.7.0] - 2026-07-23

### ระบบกะ — เฟส 2b (ปฏิทินจัดกะ)
- **หน้าใหม่ 🗓️ ปฏิทินจัดกะ (`/schedule`):** มุมมองปฏิทินรายเดือน — เลือกพนักงาน + เลือกกะ แล้วคลิกวันเพื่อลงกะ (เห็นเป็นแถบสีตามกะ) · เลื่อนเดือนได้ · ไฮไลต์วันนี้ + เสาร์-อาทิตย์
- **ลงทีเดียวทั้งเดือน:** ปุ่ม "ลงกะ จ–ศ ทั้งเดือน" / "ทุกวัน" / "ล้างทั้งเดือน" (endpoint `POST /api/admin/schedule/bulk`)
- **หัวหน้าก็ใช้ได้:** เปิดจากหน้า "บันทึกงานทีม" (ปุ่ม 🗓️ จัดกะ) — หัวหน้าเห็นเฉพาะทีมของตน · admin เข้าจากหน้า กะ/ตารางเวร · หัวหน้างาน/เจ้าหน้าที่เข้าไม่ได้ (เด้งออก)
- ทดสอบแล้ว: E2E 14 เคส (bulk/สิทธิ์หน้า/validation) + ตรวจการเรนเดอร์ปฏิทินจริง (chip กะขึ้นตรงวัน)
> ไม่มี dependency ใหม่ · `cd /opt/hr-interview && git pull && npm install --omit=dev && pm2 restart hr-interview`
>
> _เฟสถัดไป: 2c รายงานคิดชั่วโมง/วันทำงานที่คาดหวังตามกะ_

## [1.6.0] - 2026-07-23

### ระบบกะ — เฟส 2a (แม่แบบกะ + หน้าบันทึกงานอ่านชั่วโมงตามกะ)
- **หน้าใหม่ Admin › กะ/ตารางเวร:** ตั้ง **แม่แบบกะ** (ชื่อ + เข้า-ออก + พัก + สี) เพิ่ม/แก้/ลบ + **จัดกะรายวันให้พนักงาน** (เลือกคน+วันที่+กะ)
- **หน้าบันทึกงาน:** ถ้าวันนั้นถูกจัดกะ → ตารางชั่วโมงเป็นไปตามกะของวันนั้น (แสดงแบนเนอร์ "วันนี้เข้ากะ…") · วันที่ไม่ได้จัดกะ → ใช้เวลาทำงานปกติของคนนั้น (ระบบเดิมไม่พัง)
- **สิทธิ์จัดกะ:** admin + หัวหน้า (ผู้จัดการ/หัวหน้าฝ่าย/หัวหน้าแผนก) เฉพาะคนในขอบเขตตัวเอง · หัวหน้างาน/เจ้าหน้าที่ = ดูอย่างเดียว
- ทดสอบแล้ว: E2E 21 เคส (CRUD, worklog อ่านกะ, ข้ามพัก, fallback, RBAC)
> ไม่มี dependency ใหม่ · `cd /opt/hr-interview && git pull && npm install --omit=dev && pm2 restart hr-interview`
>
> _เฟสถัดไป: 2b ปฏิทินจัดกะแบบเต็ม · 2c รายงานคิดตามกะ_

## [1.5.0] - 2026-07-23

### หมวดหมู่งาน — ค้นหา + เพิ่ม/แก้ไข/ลบ + แยกตามฝ่าย
- **หน้าบันทึกงาน:** ช่องหมวดหมู่เปลี่ยนเป็น **พิมพ์ค้นหาได้** และแสดงเฉพาะหมวดของฝ่ายตัวเอง + หมวดกลาง
- **หน้าใหม่ Admin › หมวดหมู่งาน:** เพิ่ม / แก้ไข / ลบ หมวดหมู่ + กำหนดฝ่าย (เว้นฝ่าย = "หมวดกลาง" ใช้ได้ทุกฝ่าย) + ช่องค้นหา
- ของเดิม (หมวดหมู่แบบรายการชื่อ) แปลงเป็น "หมวดกลาง" อัตโนมัติ — ไม่มีข้อมูลหาย · แก้ชื่อมีผลกับงานที่กรอกใหม่ (งานเก่าคงชื่อเดิมไว้)
- พนักงานยังเพิ่มหมวดด่วนได้จากหน้าบันทึกงาน (เข้าฝ่ายตัวเอง)
- ทดสอบแล้ว: E2E 18 เคส (migration, CRUD, กันซ้ำ, กรองตามฝ่าย, quick-add, สิทธิ์)
> ไม่มี dependency ใหม่ · `cd /opt/hr-interview && git pull && npm install --omit=dev && pm2 restart hr-interview`
>
> _หมายเหตุ: ส่วนที่ 2 (ระบบกะ + ปฏิทิน) เป็นเฟสถัดไป_

## [1.4.3] - 2026-07-23

### บันทึกงาน — ใช้ง่ายขึ้นบนมือถือ + ปุ่มบันทึกรายชั่วโมง
- เพิ่มปุ่ม **"💾 บันทึกชั่วโมงนี้"** ท้ายทุกช่วงเวลา — กดบันทึกได้ทันทีไม่ต้องเลื่อนลงล่างสุด (บันทึกงานที่กรอกไว้ทั้งหมด รวมชั่วโมงนั้น) พร้อมยืนยัน "✓ บันทึกแล้ว" ตรงปุ่ม
- ปุ่มบนมือถือเต็มความกว้าง แตะง่ายขึ้น (ปุ่มเพิ่มงาน + บันทึกชั่วโมง)
- อัปเดตภาพมือถือในคู่มือสอนงาน + สร้าง PDF ใหม่
> ไม่มี dependency ใหม่ · `cd /opt/hr-interview && git pull && npm install --omit=dev && pm2 restart hr-interview`

## [1.4.2] - 2026-07-22

### แก้ UI จากการทดสอบเบราว์เซอร์
- หน้าผู้บริหารกลุ่ม: แท็บตัวอักษรสีขาวบนพื้นขาว มองไม่เห็น → ตั้งสีตัวอักษรให้อ่านได้
- หน้าจัดการผู้ใช้: dropdown ฝ่าย/แผนก/ตำแหน่ง เลือกแบบ cascade (เลือกฝ่าย → เห็นเฉพาะแผนกในฝ่ายนั้น, เลือกแผนก → เห็นเฉพาะตำแหน่งในแผนกนั้น) + security headers/health check (ops)

## [1.4.1] - 2026-07-22

### เสริมความปลอดภัย + ops (ทำได้เองระหว่างรอเพิ่มวงเงิน)

- **Security headers** ทุก response: `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY` (กัน clickjacking/ฝัง iframe), `Referrer-Policy: strict-origin-when-cross-origin` — ตามที่ audit ชี้ (ยังไม่ใส่ CSP scriptSrc เต็ม เพราะหน้าเว็บยังใช้ inline script; เป็น backlog งานใหญ่ที่ต้องรื้อ inline ก่อน)
- **Health check endpoint** `GET /healthz` (สาธารณะ ไม่ต้องล็อกอิน) คืน `{ok, service, uptime_sec}` — ไว้ให้ระบบ monitoring/Cloudflare เช็กว่าเซิร์ฟเวอร์ยังทำงาน ไม่เผยข้อมูลลับ

### สิ่งที่ต้องทำตอน Deploy
> ไม่มี dependency ใหม่
>
> ```
> cd /opt/hr-interview && git pull && npm install --omit=dev && pm2 restart hr-interview
> ```

## [1.4.0] - 2026-07-22

### บัญชีผู้บริหารกลุ่ม + กู้คืนข้อมูล + Audit log + ประวัติผลวิเคราะห์

#### บัญชีผู้บริหารกลุ่ม (Group Executive)
- บัญชีใหม่แยกต่างหาก (`/exec`) ดูข้อมูล **ทุกบริษัทแบบอ่านอย่างเดียว** — เฉพาะรายงานสรุป/ภาพรวม (dashboard สรุป, รายงานบันทึกงาน, optimization report, โครงสร้างองค์กร) **ไม่เห็น**คำตอบสัมภาษณ์รายคน/ค่าใช้จ่าย Claude/ไฟล์สำรอง
- ช่องทางแยกจากระบบเดิม (cookie `group_auth`) — **ไม่แตะกำแพงกันข้อมูลข้ามบริษัทเดิม** · เขียน/แก้ไข/แอดมินไม่ได้ทุกกรณี
- **ปิดใช้เป็นค่าเริ่มต้น** — Super Admin เปิด+ตั้งรหัสเอง (หน้า Super) · ยกเลิก/เปลี่ยนรหัส = ตัด session เดิมทันที (token version) · บังคับเปลี่ยนรหัสครั้งแรก

#### กู้คืนข้อมูล (Restore)
- ปุ่มกู้คืนในเว็บ (Admin กู้บริษัทตัวเอง / Super กู้ระดับระบบ) — **สำรองของปัจจุบันก่อนกู้อัตโนมัติ** + ตรวจไฟล์ก่อนแตก (กัน path traversal) + สลับข้อมูลแบบ atomic + แยกเฉพาะบริษัทนั้น · ต้องยืนยันด้วยการพิมพ์ RESTORE

#### Audit log (บันทึกการใช้งาน)
- บันทึกเหตุการณ์สำคัญแบบ append-only (login, จัดการผู้ใช้/องค์กร, เปลี่ยน/รีเซ็ตรหัส, สำรอง/กู้คืน, สร้าง/ลบบริษัท, ผู้บริหารกลุ่มเข้าดูบริษัทไหน) — ไม่บันทึกรหัส/ข้อมูลลับ · หน้าดูใน Admin + Super

#### ประวัติผลวิเคราะห์ (บนหน้า Dashboard Job)
- ทุกครั้งที่กด "วิเคราะห์" (รายคน/ภาพรวมบริษัท) ระบบ **เก็บผลเดิมเป็นเวอร์ชันสำรองพร้อมวันที่-เวลา** ก่อนสร้างทับ
- เมนู "เวอร์ชันเก่า" (รายบริษัท) + "ประวัติ" (รายคน) เปิดดู/ดาวน์โหลดย้อนหลังได้ · หมายเหตุ: เก็บเฉพาะที่วิเคราะห์**หลัง**เวอร์ชันนี้เป็นต้นไป

#### ปรับปรุงภายใน
- Cache การอ่านไฟล์ผู้ใช้บน hot path (ลด I/O ที่เพิ่มจาก token version)

> **QC:** ตรวจโดย orchestrator + E2E รวม 127 เคส (batch2a 48 · batch2b 66 · batch2c 13) — ผ่านทั้งหมด · การตรวจโดยทีม security อิสระยังไม่ได้รัน (เพดานวงเงิน) — แนะนำทดสอบสิทธิ์ผู้บริหารกลุ่มจริง 1 รอบก่อนเปิดใช้บนอินเทอร์เน็ต

### สิ่งที่ต้องทำตอน Deploy
> **ไม่มี dependency ใหม่** (แต่รวม v1.3.0 ที่เปลี่ยน xlsx อยู่แล้ว จึงยังควร `npm install`)
>
> ```
> cd /opt/hr-interview && git pull && npm install --omit=dev && pm2 restart hr-interview
> ```
> ผู้บริหารกลุ่มปิดอยู่โดย default — เปิดใช้ที่หน้า Super Admin เมื่อพร้อม

## [1.3.1] - 2026-07-21

### เพิ่ม Role ใหม่: "หัวหน้างาน" (supervisor)

- เพิ่มบทบาท **หัวหน้างาน** อยู่เหนือ "เจ้าหน้าที่" และใต้ "หัวหน้าแผนก"
- สิทธิ์: **ดูอย่างเดียว (read-only)** ข้อมูลของคนในแผนกตัวเอง (บันทึกงาน/สถานะสัมภาษณ์/รายงานทีม) — แก้ไข/จัดการ/วิเคราะห์/ตั้งค่าไม่ได้
- admin เลือก role นี้ได้จากหน้า "จัดการผู้ใช้" (ต้องกำหนดแผนกให้ด้วย จึงจะเห็นทีมของตน) · รองรับการนำเข้า Excel ("หัวหน้างาน")
- เชิงเทคนิค: ใช้ scope เดียวกับ section_head (ผ่าน `canView` จุดเดียว) แต่ `canEdit` คืน false และไม่มี endpoint เขียน/แอดมินใดรับ role นี้ → อ่านอย่างเดียวโดยโครงสร้าง
- ⚠️ ขอบเขต "ทีม" = ทั้งแผนกของหัวหน้างาน (ระบบยังไม่มีหน่วยย่อยกว่าแผนก)

### สิ่งที่ต้องทำตอน Deploy
> **ไม่มี dependency ใหม่** — deploy ตามปกติ
>
> ```
> cd /opt/hr-interview && git pull && npm install --omit=dev && pm2 restart hr-interview
> ```
> หมายเหตุ: การตรวจ QC อิสระ (security/scope E2E ข้ามแผนก) ยังไม่ได้รันเพราะเพดานวงเงิน — แนะนำทดสอบสิทธิ์จริง 1 รอบก่อนใช้งานหนัก

## [1.3.0] - 2026-07-20

### ตรวจทั้งระบบ (audit) + แก้ความปลอดภัย/ความทนทาน 6 จุด

รอบนี้ทีมตรวจทั้งระบบ 4 มุม (โค้ด/ทดสอบจริง/ความปลอดภัย/สถาปัตยกรรม) แล้วแก้จุดสำคัญที่พบ ผ่านการตรวจ reviewer + tester + security (ทดสอบสด รวม regression 145+ เคส)

#### แก้ไข (ความปลอดภัย + ป้องกันข้อมูลหาย)
- **บั๊กเวลา (timezone)** — "วันนี้" ของหน้าบันทึกงานเคยอิงเวลาเซิร์ฟเวอร์ (ถ้า droplet เป็น UTC จะเพี้ยนช่วงตี 0–7 โมงเช้า ทำให้กดบันทึกงานไม่ได้) → เปลี่ยนไปใช้เวลา Asia/Bangkok ทุกจุด (`todayLocal` → `nowBangkok()`)
- **เขียนไฟล์แบบ atomic + อ่านแบบ fail-loud** — เขียน JSON ผ่าน temp file แล้ว rename (กันไฟล์พังตอนเซิร์ฟเวอร์ดับ/ไฟดับกลางคัน) · ถ้าอ่านไฟล์เสีย จะเตือนใน log + สำรองไฟล์เสียเป็น `.corrupt-*` (ครั้งเดียว) ก่อนคืนค่าว่าง — กันข้อมูลหายถาวรแบบเงียบ
- **ยกเลิก session ได้ (token version)** — ลบพนักงาน/ลดสิทธิ์/เปลี่ยนรหัส → token เดิมใช้ไม่ได้ทันที (เดิมใช้ได้ต่ออีก 7 วัน) · รองรับ session เดิมที่ค้างอยู่ (backward-compatible)
- **บังคับเปลี่ยนรหัสเริ่มต้น** — ผู้ที่ยังใช้รหัส default (`super!2026` / `WWN2026!Init`) จะถูกบังคับตั้งรหัสใหม่ก่อนใช้งาน (กันทั้งฝั่งหน้าเว็บและฝั่งเซิร์ฟเวอร์) · เอารหัส default ออกจาก log · super ที่รีเซ็ตรหัส admin ให้บริษัท จะบังคับ admin เปลี่ยนรหัสด้วย
- **กัน CSV formula injection** — ย้ายการสร้าง CSV มาไว้ฝั่งเซิร์ฟเวอร์ (serializer กลาง `scripts/csv.js`) ใส่ `'` นำหน้า cell ที่ขึ้นต้นด้วย `= + - @` + RFC-4180 + BOM + CRLF · เพิ่ม endpoint `GET /api/worklog/report/csv` (สิทธิ์/ขอบเขตเท่ากับหน้ารายงาน)
- **อัปเกรด xlsx** — `0.18.5` → `0.20.3` (ปิดช่องโหว่ CVE-2023-30533 / CVE-2024-22363) + จำกัดขนาด/จำนวนแถวไฟล์อัปโหลด (กัน ReDoS)

### ⚠️ สิ่งที่ต้องทำตอน Deploy (สำคัญ — รอบนี้มี dependency ใหม่)
> เวอร์ชันนี้ **เปลี่ยน dependency (xlsx)** จึง **ต้อง `npm install`** และเซิร์ฟเวอร์ต้องเข้าถึง `cdn.sheetjs.com` ได้
>
> ```
> cd /opt/hr-interview && git pull && npm install --omit=dev && pm2 restart hr-interview
> ```
> - หลัง restart ตรวจ: `node -e "console.log(require('xlsx').version)"` ควรได้ `0.20.3`
> - แนะนำตั้ง timezone เซิร์ฟเวอร์ให้ตรงด้วย: `sudo timedatectl set-timezone Asia/Bangkok` (โค้ดยึด Asia/Bangkok อยู่แล้ว แต่ตั้งให้ตรงจะช่วยเรื่อง log/ops)
> - ถ้ายังใช้รหัส default อยู่ ครั้งแรกที่เข้าจะถูกบังคับตั้งรหัสใหม่

### หมายเหตุ (งานที่ตั้งใจทำต่อในรอบถัดไป — ไม่บล็อกรอบนี้)
- ปุ่มกู้คืนข้อมูล (restore) ในหน้าเว็บ · สำรองออกนอกเครื่อง (off-site) · audit log · cache การอ่านไฟล์ผู้ใช้บน hot path · CSP/security headers · รองรับสเกลใหญ่ (แบ่ง worklog รายเดือน/pagination)

## [1.2.3] - 2026-07-06

### แก้การแสดงผลบนมือถือ (responsive)

- **หน้าบันทึกงาน** — แถบเครื่องมือ (แถบความคืบหน้า + เมนูวันหยุด/ลา + ปุ่ม "คัดลอกงานประจำจากเมื่อวาน") บนจอมือถือถูกบีบจนตัวอักษรไทยของปุ่มแตกทีละตัว แก้ให้ตกบรรทัดเป็นระเบียบ (เพิ่ม `flex-wrap` + กันปุ่มขึ้นบรรทัดใหม่ทั้งคำ) ยืนยันแล้วที่ความกว้าง 390px และ 320px
- **หน้าสรุปบันทึกงาน** — แถวหัวข้อ "อัตราการบันทึกรายคน" + ปุ่มดาวน์โหลด CSV แก้ให้ตกบรรทัดได้บนจอแคบเช่นกัน (กันบั๊กแบบเดียวกัน)

### สิ่งที่ต้องทำตอน Deploy
> เวอร์ชันนี้ **ไม่มี dependency ใหม่** (แก้เฉพาะไฟล์หน้าเว็บ)
>
> ```
> cd /opt/hr-interview && git pull && npm install --omit=dev && pm2 restart hr-interview
> ```

## [1.2.2] - 2026-07-06

### เปลี่ยนโลโก้/ไอคอนแอปใหม่

- เปลี่ยนรูปไอคอนของระบบทั้งชุด (favicon บนแท็บเบราว์เซอร์ + ไอคอนแอปบนมือถือ PWA) เป็นโลโก้ HR-Interview ใหม่: `logo.png`, `icon-192.png`, `icon-512.png`, `icon-maskable-512.png` (ไอคอน maskable ทำพื้นน้ำเงินเต็มสำหรับ Android)
- เพิ่ม cache-busting ให้รูปใหม่แสดงทันทีหลัง deploy: ตั้ง `Cache-Control: no-cache` สำหรับไฟล์ `.png` ด้วย และติดเวอร์ชัน `?v=2` ให้ favicon ทุกหน้า + ไอคอนใน manifest + apple-touch-icon

### สิ่งที่ต้องทำตอน Deploy
> เวอร์ชันนี้ **ไม่มี dependency ใหม่** — ผู้ใช้บางคนอาจต้องปิด/เปิดแอปที่ติดตั้งบนมือถือใหม่ครั้งเดียวเพื่อให้ไอคอนแอปอัปเดต
>
> ```
> cd /opt/hr-interview && git pull && npm install --omit=dev && pm2 restart hr-interview
> ```

## [1.2.1] - 2026-07-04

### แก้บั๊ก "วิเคราะห์ภาพรวมบริษัท"

แก้ปัญหาการวิเคราะห์ภาพรวมบริษัทด้วย AI ที่ (1) รายงานมาไม่ครบเพราะคำตอบถูกตัด และ (2) ปุ่มค้างเพราะงานนานเกิน timeout ของ Cloudflare

#### แก้ไข
- **รายงานมาไม่เต็ม (คำตอบถูกตัด)** — เพิ่มเพดานความยาวคำตอบ (`max_tokens`): รายงานภาพรวมบริษัท **8000 → 20000**, เอกสารรายคน **8000 → 12000** และเพิ่มการเตือนใน log เมื่อคำตอบถูกตัดกลางคัน (`stop_reason` เป็น `max_tokens`) เพื่อให้เห็นได้ทันทีว่ารายงานอาจไม่ครบ
- **ปุ่มวิเคราะห์ค้าง (timeout ของ Cloudflare)** — เปลี่ยนปุ่ม "วิเคราะห์ภาพรวมบริษัท" ให้ทำงานเป็น **งานพื้นหลัง (background job)** แทนการรอผลในคำขอเดียว:
  - เพิ่ม endpoint สำหรับดึงสถานะงานวิเคราะห์
  - เพิ่มป็อปอัปแสดงความคืบหน้าพร้อม polling สถานะเป็นระยะ
  - รองรับ resume: ถ้าปิด/รีเฟรชหน้าแล้วเปิดกลับมาระหว่างงานยังไม่เสร็จ จะแสดงความคืบหน้าต่อได้ ไม่ต้องเริ่มใหม่
  - กันปุ่มค้างจากการที่ Cloudflare ตัดการเชื่อมต่อเมื่องานใช้เวลานาน

### สิ่งที่ต้องทำตอน Deploy
> เวอร์ชันนี้ **ไม่มี dependency ใหม่**
>
> ```
> cd /opt/hr-interview && git pull && npm install --omit=dev && pm2 restart hr-interview
> ```

## [1.2.0] - 2026-07-03

### ระบบสำรองข้อมูล (Backup)

เพิ่มระบบสำรองและกู้คืนข้อมูลในตัว ให้ผู้ดูแลกดสำรอง+ดาวน์โหลดได้เองจากหน้าเว็บ ไม่ต้องใช้คำสั่งบนเซิร์ฟเวอร์

#### เพิ่มใหม่
- **ปุ่มสำรอง + ดาวน์โหลด 2 ระดับ**
  - **Super Admin** — สำรอง **ทั้งระบบ** (ข้อมูลทุกบริษัท + ข้อมูลผู้ดูแลระดับระบบ) ที่หน้าใหม่ "สำรองข้อมูล" (`/super/backup`)
  - **Admin** — สำรอง **เฉพาะบริษัทของตัวเอง** จากหน้าผู้ดูแลระบบ · ข้อมูลแต่ละบริษัทแยกไฟล์ ไม่ปนกัน
- **auto-backup ตั้งเวลาได้** — ตั้งให้สำรองอัตโนมัติตามรอบเวลาได้ (**ปิดไว้เป็นค่าเริ่มต้น**) และกำหนดจำนวนชุดที่เก็บย้อนหลังได้ (เก็บ N ชุดล่าสุด ลบชุดเก่าอัตโนมัติ)
- **ใช้ `tar` ของระบบ (system tar)** — ไม่มี dependency ใหม่ ไม่ต้อง `npm install` เพิ่มสำหรับฟีเจอร์นี้

#### ความปลอดภัย
- **กัน path traversal** — ตรวจชื่อไฟล์/พาธก่อนอ่าน-เขียน-ดาวน์โหลด กันการหลุดออกนอกโฟลเดอร์ `backups/`
- **กันสำรองซ้อน** — ล็อกไม่ให้เริ่มงานสำรองใหม่ขณะที่งานเดิมยังทำไม่เสร็จ
- ไฟล์สำรอง (`backups/`) และการตั้งค่า (`data/_backup-settings.json`) ถูกกันไม่ให้ขึ้น git (มีข้อมูลจริงของทุกบริษัท)

#### เอกสาร
- อัปเดตเอกสาร Deploy (DigitalOcean) เพิ่มหัวข้อการสำรองข้อมูล + วิธีกู้คืนด้วยมือ + คำเตือนให้ดาวน์โหลดไฟล์สำรองไปเก็บนอกเซิร์ฟเวอร์

### สิ่งที่ต้องทำตอน Deploy
> เวอร์ชันนี้ **ไม่มี dependency ใหม่** สำหรับ backup (ใช้ system `tar`)
> แต่ยังต้อง `npm install` ตามปกติ เพราะเวอร์ชันก่อนหน้า (1.1.0) เพิ่ม `web-push` เข้ามา
>
> ```
> cd /opt/hr-interview && git pull && npm install --omit=dev && pm2 restart hr-interview
> ```

## [1.1.0] - 2026-07-03

### ระบบแจ้งเตือนกรอกงานเข้ามือถือ (PWA + Web Push)

เพิ่มความสามารถให้พนักงานได้รับการแจ้งเตือนบนมือถือเมื่อลืมกรอกงานประจำชั่วโมง โดยไม่ต้องเปิดเว็บค้างไว้

#### เพิ่มใหม่
- **ติดตั้งเว็บเป็นแอปบนมือถือ (PWA)** — เพิ่มให้ติดตั้งหน้าเว็บลงหน้าจอโฮมของมือถือได้เหมือนแอปจริง (Service Worker + ไอคอนแอป 192/512/maskable)
- **แจ้งเตือนรายชั่วโมงเฉพาะเมื่อลืมกรอกงาน** — ระบบส่ง Web Push เตือนเฉพาะชั่วโมงที่ยังไม่ได้กรอก และ **ข้ามอัตโนมัติ**: เวลาพัก, วันหยุดบริษัท (รายสัปดาห์/รายวันที่), และวันลา/วันหยุดของพนักงานรายคน
- **ปุ่มเปิด/ทดสอบการแจ้งเตือนบนมือถือ** — พนักงานกดเปิดรับการแจ้งเตือนและกดทดสอบได้เองจากหน้ากรอกงาน
- **ผู้ดูแลระบบเปิด/ปิดการแจ้งเตือนทั้งบริษัทได้** — สวิตช์ควบคุมระดับบริษัทในหน้าผู้ดูแลระบบ
- **VAPID keys** — สร้าง/เก็บกุญแจ VAPID อัตโนมัติสำหรับยืนยันตัวตนผู้ส่ง Web Push (เก็บใน `data/_vapid.json` ไม่นำขึ้น git)
- **ตัวจับเวลาส่งแจ้งเตือน (scheduler)** — ทำงานเบื้องหลังโดยยึดเวลาไทย (`Asia/Bangkok`) เสมอ วนตรวจทุก tenant

#### แก้ไข/ปรับปรุง
- เพิ่ม **Express global error handler** จับ JSON body ที่ผิดรูปแบบ (`entity.parse.failed`) ให้ตอบ 400 เป็น JSON แทน HTML stack trace (กันเผย path เซิร์ฟเวอร์)
- อัปเดตคู่มือการใช้งาน (`manual-full`, `manual-roles`) และไฟล์ PDF ให้ครอบคลุมการติดตั้งแอปและการเปิดแจ้งเตือน
- อัปเดตเอกสาร Deploy (DigitalOcean) เพิ่มขั้นตอนการติดตั้ง dependency ใหม่

#### ความปลอดภัย
- endpoint การสมัคร/ทดสอบแจ้งเตือนอ้างอิงตัวตนจาก session token เท่านั้น (กัน IDOR) และป้องกันบัญชีที่ไม่มี `user_id`

### สิ่งที่ต้องทำตอน Deploy (สำคัญ)
> เวอร์ชันนี้มี dependency ใหม่ (`web-push`) — ต้องรัน `npm install` บน droplet **ก่อน** `pm2 restart`
> มิฉะนั้นระบบแจ้งเตือนจะไม่ทำงาน (ฟีเจอร์ที่พึ่ง dependency จะปิดเงียบ)
>
> ```
> cd /opt/hr-interview && git pull && npm install && pm2 restart hr-interview
> ```
